# Parsing ~400 sources against the full platform classpath needs headroom.
org.gradle.jvmargs=-Xmx3g -XX:+UseParallelGC
# CI-friendly: no lingering daemon, no network beyond plugin/recipe resolution.
org.gradle.daemon=false
org.gradle.configuration-cache=false
