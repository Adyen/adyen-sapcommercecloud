# -----------------------------------------------------------------------
# Copyright (c) 2026 Adyen B.V. - MIT license.
# -----------------------------------------------------------------------

adyenv6chargebeeconnector.application-context=adyenv6chargebeeconnector-spring.xml

# --- Chargebee connector configuration ---
# Site, API key, gateway account id, the webhook Basic Auth credentials and the Adyen merchant account
# used by the gateway-binding check all live on the base store
# (Backoffice: Adyen Configuration > Chargebee Config).

# --- Transport tuning (stays here: per installation, not per store) ---
# Time to establish the connection.
chargebee.http.connectTimeoutMillis=5000
# Time to wait for Chargebee's response. httpclient5 has NO default here, so without this value a
# call that connects and then hangs holds a platform worker thread forever.
#
# Sized for the checkout, not for a background job: activation runs on the shopper's request thread and
# makes three sequential calls (create customer, import token, create subscription), so this value is
# multiplied by three before the shopper sees anything. Chargebee answers well under a second in normal
# operation.
#
# Exceeding it is not a lost activation: the attempt is journalled before the first call, so a timeout
# surfaces as a RetryableBillingException, the record is marked FAILED with a due date, and the retry job
# finishes it within its five-minute cycle. The shopper's wait is bounded; the work is not dropped.
#
# The same value also governs the reconciliation sweep and the retry job, where a longer wait would be
# harmless. If a slow Chargebee ever starts making those churn, split this into a separate timeout for the
# background callers rather than raising it back here and handing the checkout the bill.
chargebee.http.responseTimeoutMillis=5000
# How long a caller may queue for a pooled connection. Left at the httpclient default it would be
# three minutes, which silently overrides the two timeouts above as the real worst case.
chargebee.http.connectionRequestTimeoutMillis=5000
# Pool size, total and per route: all traffic goes to the one Chargebee host, and the library default
# of 2 per route would serialize the whole platform onto two connections.
chargebee.http.maxConnections=20
